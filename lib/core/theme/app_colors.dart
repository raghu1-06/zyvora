import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const background = Color(0xFFF0EEFF); // lavender white (screen bg)
  static const surface = Color(0xFFFFFFFF);
  static const cardBg = Color(0xFFFFFFFF);
  static const cardBorder = Color(0xFFDDD6FE);
  
  static const primary = Color(0xFF5B21B6); // violet-700
  static const primaryMid = Color(0xFF7C3AED); // violet-600 (most used)
  static const primaryLight = Color(0xFFA78BFA); // violet-400
  
  static const sectionBg = Color(0xFFE8E4F8);
  
  static const textPrimary = Color(0xFF1E1B33);
  static const textSecondary = Color(0xFF6B7280);
  static const textMuted = Color(0xFF9CA3AF);
  
  static const success = Color(0xFF10B981);
  static const successBg = Color(0xFFD1FAE5);
  static const warning = Color(0xFFF59E0B);
  static const warningBg = Color(0xFFFEF3C7);
  static const error = Color(0xFFEF4444);
  static const errorBg = Color(0xFFFEE2E2);
  
  static const navActive = Color(0xFF7C3AED);
  static const navInactive = Color(0xFF9CA3AF);
  
  static const darkBg = Color(0xFF07060F);
  static const darkSurface = Color(0xFF111128);
  static const darkCard = Color(0xFF1C1A2E);
  static const darkBorder = Color(0xFF2D2B50);
}
